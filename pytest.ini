[pytest]
testpaths = tests
markers =
    unit: fast offline unit tests
    integration: offline integration tests
    ui: browser-based UI smoke tests
    manual: human review artifacts
    slow: longer-running tests
